spring.application.name=ecommerce-cart-order-system
server.port=8080

# Set to true (or pass --app.console.enabled=true) to start the interactive
# console demo instead of relying only on the REST API.
app.console.enabled=false

logging.level.org.springframework=INFO
logging.level.com.training.ecommerce=INFO
